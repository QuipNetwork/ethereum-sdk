node_modules
.env
bun.lock

# Hardhat files
/cache
/artifacts

# Foundry files
/out
/cache_forge
/dependencies

*.tmp.json
dist/

# Generated SDK files — the abi/ directory is regenerated by
# `scripts/copy-abi.js` on each `npm run build`, EXCEPT for hand-maintained
# entries (see `EXTRA_BARREL_LINES` in that script). Those must be
# explicitly un-ignored below so they survive a clean checkout.
src/v1/abi/
!src/v1/abi/EntryPointV07.ts
src/v1/bytecode.json
.DS_Store

# test-sdk operator scripts — see test-sdk/README.md. state.json holds
# per-EOA `quantumSecret`s for the on-chain Quip wallets and MUST NOT
# be committed.
test-sdk/state.json

pwCrack.ts
.vscode/